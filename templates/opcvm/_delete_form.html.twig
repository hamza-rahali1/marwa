<form method="post" action="{{ path('o_p_c_v_m_delete', {'id': o_p_c_v_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ o_p_c_v_m.id) }}">
    <button class="btn"><i class="fas fa-trash"></i></button>
</form>
